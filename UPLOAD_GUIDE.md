# 上传指南

## 基本原则

1. 上传前确认目标目录与文件命名。
2. 不删除、不覆盖已有内容；同名文件应先确认处理方式。
3. 文档优先使用 Markdown，并保持 UTF-8 编码。
4. 补丁资料放入对应的 `docs/patches/` 编号目录。
5. 数值资料、文本资产、交接资料分别放入各自目录。
6. 废弃内容放入 `docs/deprecated/`，并记录原因与替代位置。
7. 游戏实现放入 `src/`，测试放入 `tests/`。
