# Patch 009：第七霜落与终局评分实现记录

## 完成范围

- 新增 `data/final_frost.json` 与严格加载器，登记 D49～D55 真实温度、展示标签、外勤停工目录、终霜伤害公式、每日事实阈值、D49 准备评估、六系统评分、结局分档与标签严重度；未最终封存的平衡值统一保持 `TEST_NUMERIC`。
- D49 进入第七霜落时一次性保存人口、健康、伤残与可工作人口基线，并按煤炭、食物、住房、医疗覆盖、信任、恐慌、旧城派与关键科技八项生成结构化准备结果。
- D49～D55 强制关闭狩猎小屋、伐木场、小型采煤机与小型采钢机，并暂停全部地表资源点采集；停工不消耗资源点储量，也不伪造产出。
- 接入终霜寒冷暴露、疾病恶化、治疗进度、重症、伤残、疾病死亡与寒冷死亡；所有结果均使用整数公式和既有事务日结，不引入不可复现随机分支。
- 自然死亡使用 `min(22, 12 + floor(max(0, alive - 80) / 35))` 基础软上限；疾病死亡先占额度，寒冷死亡只使用剩余额度。至少两项既有极端危机同时成立时，按用户本轮明确确认的暂行口径使用 `(base_cap * 3 + 1) // 2`。
- 超出当日软上限的疾病与寒冷死亡不从人口中扣除，人员留在原健康池；逐日记录原始值、实际值、分类溢出、基础/应用上限、危机条件与总溢出压力，并且仅在总溢出大于零时提交一次结构化事件候选。
- `grave_city` 只按累计死亡人数相对历史总人口严格大于配置中的 30% 触发，使用整数交叉比较；历史总人口包含开局及所有正式增员，旧城派出走后不减少。
- 每日保存煤炭与炉心、供热与建筑低温、食物短缺、住房暴露、医疗积压、伤病死亡及信任恐慌事实。D49～D55 记录必须连续、人口链必须一致，温度与展示标签必须匹配配置。
- D55 在既有四种硬失败检查之后，若城市仍存活，则结算煤炭/炉心、食物、住房/温度、医疗/疾病、信任/恐慌、人口/死亡六项 0～4 分，生成 0～24 总分、五档结构化结局 ID，并应用单项崩坏、死亡比例和多系统崩坏上限。
- 生成 major / defining 两级结构化结局标签接口，并把评分、标签和每日终霜事实加入机器观察；未写完整结局正文、审问文本、推荐或自动策略。

## 存档与事务

- 存档版本从 v10 升级到 v11。
- v11 保存 D49 基线、准备评估、七日结构化记录、终霜死亡合计、D55 六项评分、总分与两级标签。
- v10 → v11 只允许 D49 之前、能够确定性补入空终霜状态的存档。D49 及以后因缺少终霜基线和逐日历史而拒绝迁移，不猜测补造。
- 日结处理器仍运行在深拷贝事务内；终霜伤害、每日记录、最终评分、自动存档或最终校验任一失败时均不提交工作状态。

## 未完成与 PENDING

- `TEST_NUMERIC` 的终霜伤害、准备阈值、每日标签阈值与评分阈值仍需测试窗复核后才能视为最终平衡。
- 自然死亡软上限溢出向重症、伤残及社会情绪转换的正式公式尚未封存；当前不增加转换比例、固定信任变化或固定恐慌变化。
- “遗体系统长期崩坏”的持续天数、阈值与判定公式尚未封存，当前不会触发 `grave_city`，也没有可由外部任意置真的占位开关。
- 极端危机时 1.5 倍并向上取整是【用户本轮明确确认的暂行实现口径】，不得写成旧文档原有封存数值。
- 完整终局报告正文、结局叙事、审问、展示 UI、D56 或游戏后状态不在 Patch 009 范围，留给 Patch 010。
- 原料食物、通用饥饿阶段与事故结果等既有未封存机制没有借终霜施工自行补写。
- 未新增 AI 专属玩法、决策评分、推荐行动或自动策略。

## 验证

- 标准库 `unittest` 全量 291 项测试通过。
- `validate-config data`、命令行初始状态输出、`compileall` 与 `git diff --check` 通过。
- 越界检查确认未实现 Patch 010 的完整报告正文、审问、UI、D56 或自动策略。
