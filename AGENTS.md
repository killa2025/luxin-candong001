# 《炉心残冬》仓库协作说明

## 项目范围

本仓库用于《炉心残冬》的文档、数据、源文件与测试资料管理。

## 协作规则

- Patch 048 用户已同意科研人手测试档：只新局按每所 10 工程师满速、少人线性慢研，目标所先乘加班倍率，再取较大贡献全额与较小贡献半额。v19 保留旧局 `legacy_fixed` 科研档与历史，十分之一研究点严格持久化，完成/取消不结转。此项覆盖第三批 4.3 的新局最低实现口径，但不回改封存源文件。不得将试行值标为 FINAL；不得同时调整地图、医疗、产出、社会、寒冷或终局门槛。不进入 Patch 049。

Patch 030、Patch 031、Patch 032、Patch 033、Patch 034、Patch 035、Patch 036、Patch 037、Patch 038、Patch 039、Patch 040、Patch 041、Patch 042、Patch 043、Patch 044、Patch 045 与 Patch 046 的既有合同继续有效。

- 新增或修改内容前先阅读 `README.md`、`UPLOAD_GUIDE.md` 与 `docs/INDEX.md`。
- 未经明确授权，不删除或覆盖已有文件。
- 文档按用途放入 `docs/` 对应目录；补丁资料按编号归档。
- 游戏代码放入 `src/`，测试放入 `tests/`。Patch 022 的门槛继续标记为 `TEST_NUMERIC` 并已并入 `main`，其合并后停炉与超仓修复也已完成复审。Patch 023～045 及其复审修正均已并入 `main`。
- Patch 042 只补资源供应链不可逆缺口的机器事实：木材警告必须同时满足尚无伐木场，且当前木材加 D49 前的保守可采上界仍严格少于尚未支付的木材加工 I 与一座伐木场木材成本；当日上界按正式派员限制计算并扣除加班锁定的工人与工程师，后续日按各地表点满容量理论产出作为安全上界，以覆盖整数余数累计和尚未结算的固定增员；上界可以偏高而导致暂不报警，但不得低于任何正式可采路径并制造假阳性强警告。研究已经完成或进行中时不得重复计算已支付研究成本，已有伐木场时不报警，D49 起地表余量不得冒充可采资源。
- Patch 043 在保持 Patch 015 钢材当前派员与 D49 停工合同、不改为木材未来上界的前提下，补齐尚未支付的钢材筛选与首座小型采钢机成本；研究进行中或已完成时不得重复计算已支付成本，取消研究零返还后重新计入研究成本，已有小型采钢机时不报警。
- Patch 044 对规则查询畸形包络返回结构化字段错误与正式请求形状；未注册命令只公开完整命令目录发现方式，不做模糊行动推荐。新生成终局报告使用格式 6，只有实际存在 `sedation_city` 终局标签时才登记其未封存正文 PENDING；既有格式 5 报告必须原样严格复载。
- Patch 045 只对新建局启用 `patch045` 平衡档：同一暴露等级且同为有房或无房的人口先汇总，再结算寒冷患病整数份额；高胜最低总分为 24；社会系统 4 分要求信任至少 85、恐慌至多 15。v17 旧局迁移至 v18 时保留 `legacy_patch021` 或 `patch022` 历史档，不追溯改变寒冷结算和终局评分。正式机器标准输入、输出和错误流固定为 UTF-8。
- Patch 046 只补机器发现性：`game.overtime` 的允许建筑类型、当前实例资格与阻塞原因必须在炉律观察、正式炉律规则查询和目标拒绝结果中稳定公开；`game.use_oath_order_action.action_id` 必须明确指向既有逐项路线规则。两者只陈述合法性，不推荐行动，不改变加班或悼亡钟机制。
- Patch 046 已合并并通过第四十五次含 A/B 停工补测。Patch 047 只修之后新结算的基础煤缺额统计、公开既有降档原因及协议/追思/人数下界，并接入用户确认的 D49“守住炉城。”；旧每日事实、旧评分与已生成报告不自动重算。缺煤日只比较基础煤支付，过载不混入，木柴不替代煤炭储备评分。研究一名工程师完整基础速度不作为 Bug 修改。
- 不得借警告或协议反馈推荐行动、赠送资源或改变成本、科技、产量、地图、终霜和平衡。不得把推进前自动存档改造成主存档，不得借协议信息泄露策略。不得改变 Patch 017 的仓储边界，不得减少三张 `FINAL` 地图资源、改变社会路线、生产和事件数值。
- Patch 040 暂停 `game.triage`，Patch 041 同步暂停 `triage_law` 新签署并兼容旧档；分诊的 12 类正式规则未全部封存前不得恢复炉律或动作、不得猜测数值。不得借本轮审计停用仍有其他正式效果的炉律。
- Patch 038 的开始研究 `confirm=true` 协议和 Patch 037 的取消研究零返还、进度清零协议继续有效。不得借科技说明反推未封存效果、建筑、产出或风险。
- 悼亡钟只公开结构化累计死亡条件，不得输出“近期死亡”正文或自行新增时间机制。不得接回含旧命令写法的路线确认正文，不得改变社会路线机制或平衡。
- 旧城终局正文不适用于零实际离开或零实际资源损失时只登记 PENDING；`sedation_city` 正式触发公式未封存前不得进入运行选择。死亡处理继续使用已确认的动态死亡记录句，不重复追加同主题长文。其他 `TODO_TEXT` 仍不得自行补写。不得把推进前自动存档改造成主存档。
- Patch 021 的逐日服务事实与 Patch 020 的确定性终局文案边界继续有效。正式随机权重、无法证明的器官/灰烬叙述、AI 决策策略、图形界面、D56、Patch 049 和其他尚未分配内容均不得自行扩展。
- 废弃资料移入 `docs/deprecated/`，并说明废弃原因与替代位置。
