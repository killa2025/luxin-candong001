# 《炉心残冬》仓库协作说明

## 项目范围

本仓库用于《炉心残冬》的文档、数据、源文件与测试资料管理。

## 协作规则

Patch 030、Patch 031、Patch 032、Patch 033、Patch 034、Patch 035、Patch 036、Patch 037、Patch 038 与 Patch 039 的既有合同继续有效。

- 新增或修改内容前先阅读 `README.md`、`UPLOAD_GUIDE.md` 与 `docs/INDEX.md`。
- 未经明确授权，不删除或覆盖已有文件。
- 文档按用途放入 `docs/` 对应目录；补丁资料按编号归档。
- 游戏代码放入 `src/`，测试放入 `tests/`；Patch 022 的门槛继续标记为 `TEST_NUMERIC` 并已并入 `main`，其合并后停炉与超仓修复也已完成复审。Patch 023～039 及其复审修正均已并入 `main`；Patch 030～039 的既有文案、研究与事实边界继续有效。Patch 040 只允许暂停 `game.triage` 的实际执行并公开能力状态：命令 ID、schema 与目标正文不得删除，命令必须标记为存在但不可执行并从可执行行动中分离；直接调用兼容返回 `triage_balance_not_sealed` 且不得改变资源、冷却、人口、医疗、死亡、伤残、社会数值、终局字段或历史。医疗站与医院仍是未来合法单一目标，养护所及其他非治疗建筑不可用。分诊的 12 类正式规则未全部封存前不得恢复执行、不得猜测数值。不得改变研究扣费、单队列、进度、成本、天数、速度或已实现效果。Patch 038 的开始研究 `confirm=true` 协议和 Patch 037 的取消研究零返还、进度清零协议继续有效。不得借科技说明反推未封存效果、建筑、产出或风险。悼亡钟只公开结构化累计死亡条件，不得输出“近期死亡”正文或自行新增时间机制。不得接回含旧命令写法的路线确认正文，不得改变社会路线机制或平衡。旧城终局正文不适用于零实际离开或零实际资源损失时只登记 PENDING，`sedation_city` 正式触发公式未封存前不得进入运行选择。死亡处理继续使用已确认的动态死亡记录句，不重复追加同主题长文。其他 `TODO_TEXT` 仍不得自行补写。不得把推进前自动存档改造成主存档，不得借协议信息推荐行动、泄露策略或改变任何平衡值。不得改变 Patch 017 的仓储边界，不得减少三张 `FINAL` 地图资源、改变社会路线、生产和事件数值；新局使用 `patch022` 平衡档，v16 及更早旧档迁移后保留 `legacy_patch021` 档。Patch 021 的逐日服务事实与 Patch 020 的确定性终局文案边界继续有效；正式随机权重、无法证明的器官/灰烬叙述、AI 决策策略、图形界面、D56、Patch 041 和其他尚未分配内容均不得自行扩展。
- 废弃资料移入 `docs/deprecated/`，并说明废弃原因与替代位置。
