# 《炉心残冬》仓库协作说明

## 项目范围

本仓库用于《炉心残冬》的文档、数据、源文件与测试资料管理。

## 协作规则

- 新增或修改内容前先阅读 `README.md`、`UPLOAD_GUIDE.md` 与 `docs/INDEX.md`。
- 未经明确授权，不删除或覆盖已有文件。
- 文档按用途放入 `docs/` 对应目录；补丁资料按编号归档。
- 游戏代码放入 `src/`，测试放入 `tests/`；Patch 022 的门槛继续标记为 `TEST_NUMERIC` 并已并入 `main`，其合并后停炉与超仓修复也已完成复审。Patch 023、Patch 024、Patch 025、Patch 026、Patch 027、Patch 027 合并后修复及 Patch 028 已并入 `main`；Patch 028 合并后修复只允许为损坏的 `autosave_end_day` 快照补充稳定结构化诊断，不得把推进前快照直接改造成可继续游戏的主存档。不得自行改动旧城事件映射，不得补写 `TODO_TEXT`、借协议信息推荐行动、泄露策略或改变任何平衡值。不得改变 Patch 017 的仓储边界，不得减少三张 `FINAL` 地图资源、改变社会路线、生产、科技和事件数值；新局使用 `patch022` 平衡档，v16 及更早旧档迁移后保留 `legacy_patch021` 档。Patch 021 的逐日服务事实与 Patch 020 的确定性终局文案边界继续有效；009-C 完整路线长文、正式随机权重、无法证明的器官/灰烬叙述、AI 决策策略、图形界面、D56 和任何尚未分配的后续 Patch 均不得自行扩展。
- 废弃资料移入 `docs/deprecated/`，并说明废弃原因与替代位置。
