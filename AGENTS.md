# 《炉心残冬》仓库协作说明

## 项目范围

本仓库用于《炉心残冬》的文档、数据、源文件与测试资料管理。

## 协作规则

- 新增或修改内容前先阅读 `README.md`、`UPLOAD_GUIDE.md` 与 `docs/INDEX.md`。
- 未经明确授权，不删除或覆盖已有文件。
- 文档按用途放入 `docs/` 对应目录；补丁资料按编号归档。
- 游戏代码放入 `src/`，测试放入 `tests/`；Patch 022 的门槛继续标记为 `TEST_NUMERIC` 并已并入 `main`，其合并后停炉与超仓修复也已完成复审。Patch 023～034 及其复审修正均已并入 `main`，Patch 030、Patch 032、Patch 033、Patch 034 分别通过第二十八次至第三十一次黑盒验收，Patch 031 已完成事件正文接线。Patch 035 只允许接入已封存的 7 条路线互斥、设施运行、炉约冷却、旧城派与熟食条件反馈；悼亡钟只公开结构化累计死亡条件，不得输出“近期死亡”正文或自行新增时间机制。不得接回含旧命令写法的路线确认正文，不得改变确认协议、社会路线机制或平衡。旧城终局正文不适用于零实际离开或零实际资源损失时只登记 PENDING，`sedation_city` 正式触发公式未封存前不得进入运行选择。死亡处理继续使用已确认的动态死亡记录句，不重复追加同主题长文。其他 `TODO_TEXT` 仍不得自行补写。不得把推进前自动存档改造成主存档，不得借协议信息推荐行动、泄露策略或改变任何平衡值。不得改变 Patch 017 的仓储边界，不得减少三张 `FINAL` 地图资源、改变社会路线、生产、科技和事件数值；新局使用 `patch022` 平衡档，v16 及更早旧档迁移后保留 `legacy_patch021` 档。Patch 021 的逐日服务事实与 Patch 020 的确定性终局文案边界继续有效；正式随机权重、无法证明的器官/灰烬叙述、AI 决策策略、图形界面、D56 和任何尚未分配的后续 Patch 均不得自行扩展。
- 废弃资料移入 `docs/deprecated/`，并说明废弃原因与替代位置。
